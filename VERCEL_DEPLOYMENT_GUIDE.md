# راهنمای استقرار در Vercel 🚀

این پروژه برای استقرار (Deploy) در هاست رایگان **Vercel** بهینه‌سازی شده است. تمامی تنظیمات لازم در فایل `vercel.json` قرار داده شده‌اند تا برنامه بدون مشکل اجرا شود.

## مراحل استقرار در Vercel:

۱. وارد حساب کاربری خود در [Vercel](https://vercel.com) شوید.
۲. روی دکمه **Add New...** و سپس **Project** کلیک کنید.
۳. مخزن (Repository) گیت‌هاب خود که این پروژه در آن قرار دارد را انتخاب کنید (Import).
۴. در بخش **Configure Project**:
   - **Framework Preset**: به صورت خودکار روی `Angular` تنظیم می‌شود (اگر نشد، دستی انتخاب کنید).
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
۵. **بسیار مهم (Environment Variables):**
   در بخش **Environment Variables**، باید کلیدهای API خود را وارد کنید تا برنامه در محیط Vercel به درستی کار کند:
   - نام متغیر: `GEMINI_API_KEY`
   - مقدار: کلید API جمینای خود را اینجا وارد کنید.
۶. روی دکمه **Deploy** کلیک کنید.

## بهینه‌سازی‌های انجام شده:
- فایل `vercel.json` برای مدیریت صحیح مسیرها (Routing) در برنامه‌های Single Page Application (SPA) انگولار ایجاد شده است تا در صورت رفرش کردن صفحه با خطای 404 مواجه نشوید.
- دستورات Build به گونه‌ای تنظیم شده‌اند که متغیرهای محیطی به درستی جایگزین شوند.

موفق باشید! 🌟
